# Dockerfile que extiende la imagen oficial ogaston/inference-g3:latest
# para agregar las dependencias necesarias para ARM64 y MySQL

FROM ogaston/inference-g3:latest

# Instalar dependencias adicionales necesarias
RUN pip install --no-cache-dir cryptography

# El resto de la configuración se mantiene igual
# La imagen base ya tiene todo lo necesario para el modelo Covertype
